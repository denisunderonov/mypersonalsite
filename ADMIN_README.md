# Админ-панель для сайта denisunderonov

## 🎯 Что реализовано

### Полная админ-панель с аутентификацией
- ✅ Вход по логину: `denisunderonov@admin.com` / пароль: `Denimz13141314..`
- ✅ Защищённые роуты (middleware auth)
- ✅ Дашборд со статистикой (количество статей, проектов, фото)
- ✅ Единый дизайн в стилистике основного сайта

### Управление статьями (Articles)
- ✅ CRUD: создание, редактирование, удаление
- ✅ Категории: IT, Дизайн, Музыка, Искусство
- ✅ Загрузка изображений для статей
- ✅ Slug (авто-генерация или ручной ввод)
- ✅ Публикация/черновик
- ✅ Публичная страница со списком статей по категориям
- ✅ Отдельная страница для каждой статьи

### Управление проектами (Projects)
- ✅ CRUD: создание, редактирование, удаление
- ✅ Категории: Большие, Учебные, Другие
- ✅ Теги (через запятую)
- ✅ Ссылки на сайт и GitHub
- ✅ Загрузка изображений
- ✅ Публикация/черновик
- ✅ Публичная страница с проектами, сгруппированными по категориям

### Управление фотографиями (Photos)
- ✅ CRUD: загрузка, редактирование, удаление
- ✅ Название и описание (опционально)
- ✅ Загрузка изображений (до 10 МБ)
- ✅ Публикация/скрытие
- ✅ Галерея в разделе "Фотографии" блога (grid-layout)

## 🚀 Как использовать

### Запуск серверов (уже запущено)
```bash
# PHP (Laravel) dev-server
php artisan serve --host=127.0.0.1 --port=8000

# Vite (assets + HMR)
npm run dev
```

### Доступ к админ-панели
1. Перейдите на: http://127.0.0.1:8000/admin/login
2. Войдите с данными:
   - Email: `denisunderonov@admin.com`
   - Пароль: `Denimz13141314..`
3. После входа вы окажетесь на дашборде

### Маршруты админки
- `/admin/login` — Вход
- `/admin/dashboard` — Главная страница админки
- `/admin/articles` — Список статей
- `/admin/articles/create` — Создать статью
- `/admin/articles/{id}/edit` — Редактировать статью
- `/admin/projects` — Список проектов
- `/admin/projects/create` — Создать проект
- `/admin/projects/{id}/edit` — Редактировать проект
- `/admin/photos` — Список фотографий
- `/admin/photos/create` — Загрузить фото
- `/admin/photos/{id}/edit` — Редактировать фото

### Публичные страницы
- `/blog` — Список статей по категориям + галерея фото
- `/blog/{slug}` — Отдельная статья
- `/projects` — Список проектов по категориям

## 🎨 Дизайн

Админ-панель стилизована под основной сайт:
- Тёмный фон (#2d3035)
- Белый текст
- Шрифты: Space Mono (заголовки), Inter (текст)
- Цветные акценты для кнопок и категорий (синий, жёлтый, циан и т.д.)
- Анимации при hover
- Адаптивный дизайн

## 📦 База данных

### Таблицы
1. **articles**: id, title, slug, content, category, image, published, timestamps
2. **projects**: id, title, slug, description, category, tags (json), image, site_url, github_url, published, timestamps
3. **photos**: id, title, image, description, published, timestamps

### Сидер
- AdminUserSeeder создаёт админ-пользователя при запуске `php artisan db:seed`

## 🖼️ Загрузка изображений

- Изображения сохраняются в `storage/app/public/`
- Symlink создан: `public/storage` → `storage/app/public`
- Доступ через `asset('storage/путь')`
- Автоматическое удаление при удалении записи

## ✨ Дополнительные возможности

- **Авто-генерация slug** из заголовка (если не указан вручную)
- **Валидация**: обязательные поля, уникальные slug, форматы изображений
- **Flash-сообщения**: успех/ошибка после операций
- **Пагинация**: по 20 элементов на страницу
- **Фильтры published**: только опубликованные элементы на публичных страницах

## 🔧 Что можно добавить в будущем

- Редактор WYSIWYG для контента статей (TinyMCE, Quill)
- Массовая загрузка фотографий
- Поиск и фильтры в админке
- Роли и права доступа (если будет несколько админов)
- Превью перед публикацией
- SEO-поля (meta description, keywords)

---

**Всё готово к работе! Заходите в админку и начинайте добавлять контент.**
