# 🚀 Деплой на Railway.app

Пошаговая инструкция по развертыванию Laravel приложения на Railway.

## 📋 Предварительные требования

- [x] Проект залит на GitHub
- [x] Аккаунт на [railway.app](https://railway.app)
- [x] Файлы `Procfile` и `nixpacks.toml` в корне проекта

## 🎯 Шаг 1: Подготовка GitHub репозитория

1. Убедись что все изменения закоммичены:
```bash
git add .
git commit -m "Prepare for Railway deployment"
git push origin main
```

## 🚂 Шаг 2: Создание проекта на Railway

1. Открой [railway.app](https://railway.app)
2. Нажми **"Start a New Project"**
3. Выбери **"Deploy from GitHub repo"**
4. Авторизуй Railway доступ к твоему GitHub
5. Выбери репозиторий **mypersonalsite**

## 🗄️ Шаг 3: Добавление базы данных PostgreSQL

1. В твоем Railway проекте нажми **"+ New"**
2. Выбери **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. Переменные окружения (`PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`) будут автоматически доступны

## ⚙️ Шаг 4: Настройка переменных окружения

В разделе **Variables** твоего сервиса добавь:

```env
APP_NAME=Denis Underonov
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:СГЕНЕРИРУЙ_КЛЮЧ
APP_URL=https://твой-домен.up.railway.app

APP_LOCALE=ru
APP_FALLBACK_LOCALE=ru

LOG_LEVEL=error

SESSION_DRIVER=database
SESSION_SECURE_COOKIE=true

DB_CONNECTION=pgsql
```

### 🔑 Генерация APP_KEY

Запусти локально:
```bash
php artisan key:generate --show
```

Скопируй полученный ключ (с префиксом `base64:`) в переменную `APP_KEY` на Railway.

## 🌐 Шаг 5: Настройка домена

1. В разделе **Settings** → **Domains**
2. Нажми **"Generate Domain"**
3. Railway создаст домен типа `your-app.up.railway.app`
4. Обнови переменную `APP_URL` с этим доменом

## 🎨 Шаг 6: Первый деплой

Railway автоматически:
1. Установит зависимости (`composer install`, `npm ci`)
2. Соберет фронтенд (`npm run build`)
3. Закеширует конфиги Laravel
4. Запустит миграции
5. Создаст symlink для storage

Процесс займет 3-5 минут.

## 👤 Шаг 7: Создание админ-пользователя

После успешного деплоя создай админа через Railway CLI или добавь в Procfile:

**Вариант 1: Через Railway CLI**
```bash
railway login
railway link
railway run php artisan db:seed --class=AdminUserSeeder
```

**Вариант 2: Добавить в Procfile (перед serve)**
```
release: php artisan migrate --force && php artisan db:seed --class=AdminUserSeeder --force
web: php artisan storage:link && php artisan serve --host=0.0.0.0 --port=$PORT
```

## 🔍 Проверка работы

1. Открой сгенерированный домен Railway
2. Перейди на `/admin/login`
3. Войди с credentials:
   - Email: `denisunderonov2@gmail.com`
   - Пароль: `Denimz13141314..`

## 🐛 Отладка проблем

### Просмотр логов
В Railway проекте открой вкладку **"Deployments"** → выбери последний деплой → **"View Logs"**

### Частые проблемы

**1. Ошибка "No application encryption key"**
- Убедись что `APP_KEY` установлен в переменных окружения

**2. Стили не загружаются**
- Проверь что `npm run build` выполнился успешно
- Проверь `APP_URL` - должен совпадать с Railway доменом

**3. База данных не подключается**
- Убедись что PostgreSQL сервис запущен
- Railway автоматически связывает переменные `PG*`

**4. 500 ошибка**
- Проверь логи: `railway logs`
- Убедись что миграции прошли успешно

## 🔄 Автоматические обновления

После настройки каждый `git push` в `main` ветку будет автоматически деплоить изменения на Railway!

```bash
# Внеси изменения в код
git add .
git commit -m "Update feature"
git push origin main
# Railway автоматически задеплоит изменения
```

## 💾 Backup базы данных

Railway автоматически делает бэкапы PostgreSQL. Для ручного экспорта:

```bash
railway connect postgres
# Откроется psql консоль
\dt  # Список таблиц
\q   # Выход
```

## 📊 Мониторинг

Railway показывает:
- CPU и RAM использование
- Количество запросов
- Время ответа
- Логи в реальном времени

## 🎉 Готово!

Твой сайт теперь доступен онлайн! 

**Твой админ-панель:** `https://your-app.up.railway.app/admin/login`

---

## 📝 Полезные команды Railway CLI

```bash
# Установка Railway CLI
npm i -g @railway/cli

# Вход в аккаунт
railway login

# Подключение к проекту
railway link

# Просмотр логов
railway logs

# Выполнение команд
railway run php artisan tinker
railway run php artisan migrate:status

# Открыть проект в браузере
railway open
```
